import type { SignInProps, SignUpProps } from "../../_types/user.types"

export const postSignIn = (params: SignInProps) => {
    // 로그인 관련 요청 로직
}

export const postSignUp = (params: SignUpProps) => {
    // 회원가입 관련 요청 로직
}

